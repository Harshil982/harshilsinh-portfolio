// Temporary display flags — flip back once ready.

// No real photo uploaded yet, so the generated monogram avatar is hidden in
// the Hero and About sections until a real headshot replaces it.
export const SHOW_PERSONAL_AVATAR = false;
